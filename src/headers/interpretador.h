//Header para interpretação do comando digitado pelo usuário
#include<string>
struct info
{
  std::string ip;
  int porta;
  int funcao;
  bool is_valid;
};

info interpretador(char comando[command_size])
{
  info _info;
  return _info;
}
